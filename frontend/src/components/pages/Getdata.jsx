import React from 'react';
import { Typography, Button } from '@mui/material';

const GetDataPage = () => {
  return (
    <div>
      <Typography variant="h4">Get Data</Typography>
      <Button variant="contained" color="primary">
        Retrieve Data
      </Button>
    </div>
  );
};

export default GetDataPage;
